import React from 'react';

const SortUnit = ({ type }) => {
	return (
		<div className={`sort-unit ${type}`}>
			<span></span>
			<span></span>
			<span></span>
		</div>
	);
};

export default SortUnit;